import PageStub from "@/components/dev/PageStub";

// T5 — CONTENT-PLAN.md §9, §11.9. Covers /dsc/documents-required, the
// /dsc/drivers hub, and the four individual driver pages. No animated hero,
// no marketing chrome — LCP target under 1.2s (DESIGN.md §11.9).
export default function UtilityPage(props) {
  return <PageStub {...props} />;
}
