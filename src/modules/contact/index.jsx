import PageStub from "@/components/dev/PageStub";

// T7 — CONTENT-PLAN.md §11. EmailJS + honeypot + time-gate + rate limit,
// built in Phase 8.
export default function Contact(props) {
  return <PageStub {...props} />;
}
