import PageStub from "@/components/dev/PageStub";

// T3 — CONTENT-PLAN.md §8. Data-driven from src/content/services once
// Phase 3/6 land; renders a stub until then.
export default function CategoryHub(props) {
  return <PageStub {...props} />;
}
