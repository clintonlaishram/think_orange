import PageStub from "@/components/dev/PageStub";

// Top-level /services — variant of CategoryHub per CONTENT-PLAN.md §8:
// lists all six categories with children inline. Built in Phase 6.
export default function ServicesHub(props) {
  return <PageStub {...props} />;
}
