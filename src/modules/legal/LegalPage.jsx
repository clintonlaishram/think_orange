import PageStub from "@/components/dev/PageStub";

// T8 — CONTENT-PLAN.md §12. One prose renderer, five content files.
// Content comes from the client's CA/lawyer in Phase 11 — never AI-drafted.
export default function LegalPage(props) {
  return <PageStub {...props} />;
}
