import PageStub from "@/components/dev/PageStub";

// T6 — CONTENT-PLAN.md §10. Built in Phase 8; blocked on Phase 11 for real
// team/office photography per IMAGE-PLAN.md — renders a typographic
// fallback in the interim, never stock people (IMAGE-PLAN.md §6).
export default function About(props) {
  return <PageStub {...props} />;
}
